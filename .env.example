# Copy this file to .env and fill in your key.
# Exposed to the browser bundle (Vite requires the VITE_ prefix) — do not
# commit a real key, and be aware anyone using the built app can read it.
VITE_ANTHROPIC_API_KEY=
